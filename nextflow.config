// Environment-specific profiles
profiles {

  saga {

    process {

      executor = "slurm"
      clusterOptions = "--account=nn10082k"
      queue = "normal"

      // This process label must be defined in your Nextflow profile
      withLabel: "require_gpu" {
        clusterOptions = "--account=nn10082k --gpus=1"
        queue = "a100"
        cpus = 8
      }

      // This process label must be defined in your Nextflow profile
      withLabel: "high_mem_per_cpu" {
        queue = "bigmem"
      }

      // This process label must be defined as below in your Nextflow profile
      withLabel: "require_pipefail" {
        shell = ["/bin/bash", "-euo", "pipefail"]
      }

    }

    executor {

      queueSize = 250

    }

    singularity {

      enabled = true
      autoMounts = true
      cacheDir = "work/containers"

    }

  }

}

// Common settings
report.overwrite = true
