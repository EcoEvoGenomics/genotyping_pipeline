process {

  executor = "slurm"

  // Saga: set to NRIS project account
  clusterOptions = "--account=nn10082k"

  // Saga: GPU jobs should run on partition a100
  withTag: "gpu" {
    queue = "a100"
  }

}

singularity {

  enabled = true
  autoMounts = true
  cacheDir = "work/containers"

}
